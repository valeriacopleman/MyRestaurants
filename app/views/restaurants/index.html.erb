<h1>My Restaurants</h1>
<% if params[:category_id]%>
    <h2>Category: <%= @category.name %></h2>
    <% @restaurants.each do |restaurant| %>
    <li><%= restaurant.name %> - <%= link_to 'Edit', edit_restaurant_path(restaurant) %></li><br>
    <% end %>
 <%end %>
<br>
<br>

<button><%= link_to 'Add Restaurant', new_category_restaurant_path %></button>
<button><%= link_to 'Back To Hone', root_path %></button>